## Packages
date-fns | Date manipulation and formatting for week selection
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
Backend API expects dates in YYYY-MM-DD format strings.
Times are in HH:mm 24-hour format strings.
Using standard Shadcn UI components that are already present in the project structure.
